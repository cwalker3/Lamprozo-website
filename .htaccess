# Deny direct web access to any log file in wp-content (debug.log,
# sync-debug.log, etc.). Firefly framework logs live here; none are public.
<FilesMatch "\.log$">
    Require all denied
</FilesMatch>
